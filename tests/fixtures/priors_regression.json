[
  {
    "id": "parameter_range_leg_length",
    "query": "What is a typical leg length (leg_length) used in Bi2Te3 thermoelectric cooler design — range or optimal value?",
    "category": "parameter_range",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 5 priors, kind=parameter_range, real DOIs. min_priors kept at 3 as a conservative floor below the observed 5.",
    "min_priors": 3,
    "must_have_kinds": ["parameter_range"],
    "must_cite_real_doi": true
  },
  {
    "id": "parameter_range_conductor_thickness",
    "query": "What conductor/metallization layer thickness (d_conductor) is typically used in Bi2Te3 thermoelectric leg fabrication?",
    "category": "parameter_range",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 3 priors, kind=parameter_range, real DOIs. Replaces material_property_zt: material_property is now permanently unproducible (material is fixed Bi2Te3, prior_target=false in sim_params.json — see spec §3.6), so a ZT/material-property query can never satisfy must_have_kinds=['material_property'] anymore. min_priors raised from the 1-placeholder to 2, a conservative floor below the observed 3.",
    "min_priors": 2,
    "must_have_kinds": ["parameter_range"],
    "must_cite_real_doi": true
  },
  {
    "id": "scaling_relationship_leg_aspect_ratio",
    "query": "How does the optimal leg length (leg_length) depend on leg width (leg_width) in Bi2Te3 thermoelectric cooler design?",
    "category": "scaling_relationship",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 0 priors. NOT a pipeline bug — PaperQA2's own synthesized answer explicitly states the corpus discusses leg height/area-ratio effects on performance generally, but no paper ties leg_length specifically to leg_width as a pairwise relationship, so nothing groundable was extracted. Reclassified from a required-coverage fixture to a verified zero-coverage one (allow_zero_priors=true). must_have_kinds kept for documentation of original intent (this fixture was meant to probe scaling_relationship) even though check_fixture()'s allow_zero_priors branch doesn't enforce it. KNOWN GAP: as of this run, no fixture in this file verifies the pipeline can actually produce a scaling_relationship prior — worth revisiting with a different query or corpus expansion.",
    "min_priors": 0,
    "must_have_kinds": ["scaling_relationship"],
    "must_cite_real_doi": true,
    "allow_zero_priors": true
  },
  {
    "id": "candidate_config_heat_sink",
    "query": "What heat sink fin geometries (fin height, fin thickness, fin count) are reported to improve thermoelectric cooler performance?",
    "category": "candidate_config",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 0 priors. NOT a pipeline bug — PaperQA2's own synthesized answer explicitly states the corpus describes fin shapes qualitatively (which shape performs better) without reporting actual height/thickness/count numbers, so nothing groundable was extracted. Reclassified from a required-coverage fixture to a verified zero-coverage one (allow_zero_priors=true). must_have_kinds kept for documentation of original intent even though check_fixture()'s allow_zero_priors branch doesn't enforce it. KNOWN GAP: as of this run, no fixture in this file verifies the pipeline can actually produce a candidate_config prior — worth revisiting with a different query or corpus expansion.",
    "min_priors": 0,
    "must_have_kinds": ["candidate_config"],
    "must_cite_real_doi": true,
    "allow_zero_priors": true
  },
  {
    "id": "caution_max_cop_limits",
    "query": "What geometric design limits (leg length, leg width, leg spacing) constrain the maximum achievable COP in Bi2Te3 thermoelectric cooling?",
    "category": "caution",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 3 priors, kind=caution, real DOIs. min_priors kept at 2, a conservative floor below the observed 3.",
    "min_priors": 2,
    "must_have_kinds": ["caution"],
    "must_cite_real_doi": true
  },
  {
    "id": "application_battery_thermal_management",
    "query": "What thermoelectric cooler leg and heat sink geometry has been reported for battery thermal management applications?",
    "category": "application",
    "notes": "Verified 2026-07-31 real run under evidence_k=20 + MIN_EVIDENCE_RELEVANCE=0.6 (spec §3.7): 3 priors, kind=candidate_config, real DOIs (leg geometry itself came back unreported in this run, but heat sink fin findings grounded fine). min_priors kept at 2, a conservative floor below the observed 3.",
    "min_priors": 2,
    "must_have_kinds": ["candidate_config"],
    "must_cite_real_doi": true
  },
  {
    "id": "edge_zero_coverage_heat_sink_interface",
    "query": "What is the typical thermal conductance of a thermoelectric cooler heat sink interface?",
    "category": "edge",
    "notes": "Verified genuine zero-coverage query (confirmed three times now: the original 100-query probe, the 42-query rerun, and the 2026-07-31 contract-scoped regression run all returned 0 priors). Asserts the pipeline degrades gracefully — empty-but-valid response with an explanatory gap, not an error. Thermal conductance of an interface isn't a sim_params.json geometry_free target either, so this stays a valid zero-coverage probe under the new contract-scoped extraction too.",
    "min_priors": 0,
    "must_have_kinds": [],
    "must_cite_real_doi": false,
    "allow_zero_priors": true
  }
]
