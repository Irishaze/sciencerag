[
  {
    "id": "parameter_range_leg_length",
    "query": "What is a typical leg length (leg_length) used in Bi2Te3 thermoelectric cooler design — range or optimal value?",
    "category": "parameter_range",
    "notes": "Captured 2026-07 real run (pre sim-contract sync): 6 priors incl. parameter_range, 2 distinct DOIs. leg_length is a core sim_params.json geometry_free target, so this fixture should still hold under the contract-scoped extraction (spec: sync_to_claude_code.md) — but re-run scripts/run_regression.py to confirm before trusting the number.",
    "min_priors": 3,
    "must_have_kinds": ["parameter_range"],
    "must_cite_real_doi": true
  },
  {
    "id": "parameter_range_conductor_thickness",
    "query": "What conductor/metallization layer thickness (d_conductor) is typically used in Bi2Te3 thermoelectric leg fabrication?",
    "category": "parameter_range",
    "notes": "PLACEHOLDER, needs a real scripts/run_regression.py run to calibrate min_priors. Replaces material_property_zt: material_property is now permanently unproducible (material is fixed Bi2Te3, prior_target=false in sim_params.json — see spec: sync_to_claude_code.md §6.1), so a ZT/material-property query can never satisfy must_have_kinds=['material_property'] anymore. d_conductor is a real sim_params.json geometry_free target.",
    "min_priors": 1,
    "must_have_kinds": ["parameter_range"],
    "must_cite_real_doi": true
  },
  {
    "id": "scaling_relationship_leg_aspect_ratio",
    "query": "How does the optimal leg length (leg_length) depend on leg width (leg_width) in Bi2Te3 thermoelectric cooler design?",
    "category": "scaling_relationship",
    "notes": "PLACEHOLDER, needs a real scripts/run_regression.py run to calibrate min_priors. Replaces scaling_relationship_fan_speed: fan speed (v_air) is an operating_condition, out of scope for priors under the new contract (spec: sync_to_claude_code.md §3) and the prompt now explicitly tells the LLM not to extract it. leg_length-depends-on-leg_width is the spec's own worked example for scaling_relationship + related_fields.",
    "min_priors": 1,
    "must_have_kinds": ["scaling_relationship"],
    "must_cite_real_doi": true
  },
  {
    "id": "candidate_config_heat_sink",
    "query": "What heat sink fin geometries (fin height, fin thickness, fin count) are reported to improve thermoelectric cooler performance?",
    "category": "candidate_config",
    "notes": "Captured 2026-07 real run (pre sim-contract sync): 13 priors incl. candidate_config, 3 distinct DOIs. Heat sink fin dimensions map onto sink_fin_h/sink_fin_w/sink_fin_n, real sim_params.json geometry_free targets, so this should still be reachable — but the count will likely drop under the narrower, target-oriented prompt. Re-run scripts/run_regression.py to recalibrate min_priors (lowered from 7 to 2 as a conservative placeholder pending that run).",
    "min_priors": 2,
    "must_have_kinds": ["candidate_config"],
    "must_cite_real_doi": true
  },
  {
    "id": "caution_max_cop_limits",
    "query": "What geometric design limits (leg length, leg width, leg spacing) constrain the maximum achievable COP in Bi2Te3 thermoelectric cooling?",
    "category": "caution",
    "notes": "Captured 2026-07 real run (pre sim-contract sync): 16 priors spanning all 5 kinds incl. material_property, 4 distinct DOIs — that material_property contribution is now impossible (prior_target=false), and the query wording was narrowed from a generic 'what limits COP' to specific geometry parameters so it stays inside the contract's 12 target parameters. Re-run scripts/run_regression.py to recalibrate min_priors (lowered from 9 to 2 as a conservative placeholder pending that run).",
    "min_priors": 2,
    "must_have_kinds": ["caution"],
    "must_cite_real_doi": true
  },
  {
    "id": "application_battery_thermal_management",
    "query": "What thermoelectric cooler leg and heat sink geometry has been reported for battery thermal management applications?",
    "category": "application",
    "notes": "Captured 2026-07 real run (pre sim-contract sync): 21 priors, 3 distinct DOIs — that run answered the open-ended 'how are TECs used in battery thermal management' question, which likely surfaced non-geometry findings (control strategy, system integration) no longer extractable under the contract-scoped prompt. Wording narrowed to explicitly ask about leg/heat-sink geometry so it stays inside the 12 target parameters. Re-run scripts/run_regression.py to recalibrate min_priors (lowered from 10 to 2 as a conservative placeholder pending that run).",
    "min_priors": 2,
    "must_have_kinds": ["candidate_config"],
    "must_cite_real_doi": true
  },
  {
    "id": "edge_zero_coverage_heat_sink_interface",
    "query": "What is the typical thermal conductance of a thermoelectric cooler heat sink interface?",
    "category": "edge",
    "notes": "Verified genuine zero-coverage query (confirmed twice: original 100-query probe and the 42-query rerun both returned 0 priors with 20 evidence contexts, all below MIN_EVIDENCE_RELEVANCE). Asserts the pipeline degrades gracefully — empty-but-valid response with an explanatory gap, not an error. Thermal conductance of an interface isn't a sim_params.json geometry_free target either, so this stays a valid zero-coverage probe under the new contract-scoped extraction too.",
    "min_priors": 0,
    "must_have_kinds": [],
    "must_cite_real_doi": false,
    "allow_zero_priors": true
  }
]
