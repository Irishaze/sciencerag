# Legacy files

This directory contains the superseded HTTP server, standalone UI builder,
HTML predictors, and earlier model artifacts. The active application entry is
`../prediction_server.py`; legacy files are retained only for reproducibility.
