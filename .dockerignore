.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.git/
.pqa_index/
logs/
data/
tests/
scripts/
docs/
tec_surrogate/legacy/
tec_surrogate/data/raw/
.env
.env.*
!.env.example

# frontend build artifacts — the Dockerfile's frontend-build stage runs its
# own `npm install`/`npm run build` inside the container; copying the
# host's node_modules/dist in would risk platform-mismatched native
# binaries and stale output.
frontend/node_modules/
frontend/dist/
