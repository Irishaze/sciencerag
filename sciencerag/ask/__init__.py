"""sciencerag.ask (spec §6) — GraphRAG-style Q&A over the knowledge graph,
with a documented fallback to component A's literature retrieval when the
graph has no matching triples yet (spec §6.2 cold-start path)."""
